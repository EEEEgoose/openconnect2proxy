# config/sockd.conf

# 日志输出到标准错误流
logoutput: stderr

# 内部网络接口（Docker 容器的默认网卡）
internal: eth0 port = 1080

# 外部网络接口（OpenConnect 创建的 VPN 通道）
external: tun0

# 使用哪个用户运行
user.unprivileged: nobody

# SOCKS 方法，这里我们不需要认证
socksmethod: none

# 客户端规则：允许所有客户端连接
client pass {
    from: 0.0.0.0/0 to: 0.0.0.0/0
    log: error connect disconnect
}

# 服务端规则：允许客户端通过代理访问任何地方
socks pass {
    from: 0.0.0.0/0 to: 0.0.0.0/0
    log: error connect disconnect
}
